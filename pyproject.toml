[build-system]
requires = ["setuptools>=42.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--cov=pbkdvault --cov-report=xml:coverage.xml --cov-report=term"
testpaths = [
    "tests",
]

[tool.pylint.basic]
good-names = ["fp"]